<Page
    x:Class="EdgeRebuild.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标签栏容器（代码动态填充） -->
        <StackPanel x:Name="TabBarPanel" Grid.Row="0" Orientation="Horizontal"
                    Height="36" Background="#FFE0E0E0"/>

        <!-- 地址栏 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="4">
            <TextBlock x:Name="EngineLabel" FontSize="16" FontWeight="Bold"
                       VerticalAlignment="Center" Margin="0,0,6,0"/>
            <Button x:Name="BackBtn" Content="◀" Width="30" IsEnabled="False"
                    Click="BackBtn_Click"/>
            <Button x:Name="ForwardBtn" Content="▶" Width="30" IsEnabled="False"
                    Click="ForwardBtn_Click"/>
            <Button x:Name="RefreshBtn" Content="↻" Width="30"
                    Click="RefreshBtn_Click"/>
            <TextBox x:Name="UrlBox" Width="400" PlaceholderText="输入网址或搜索"
                     KeyDown="UrlBox_KeyDown"/>
            <Button Content="+ 新标签" Click="NewTabBtn_Click"/>
            <ComboBox x:Name="EngineCombo" Width="100" SelectedIndex="0">
                <ComboBoxItem Content="EdgeHTML"/>
                <ComboBoxItem Content="WebView2"/>
            </ComboBox>
        </StackPanel>

        <!-- 内容区域 -->
        <Border x:Name="ContentContainer" Grid.Row="2" Margin="4"/>
    </Grid>
</Page>