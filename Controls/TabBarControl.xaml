<UserControl
    x:Class="EdgeRebuild.Controls.TabBarControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <UserControl.Resources>
        <SolidColorBrush x:Key="MutedForegroundBrush" Color="#666666"/>
        <Style x:Key="TabToolButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource MutedForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal"/>
                                    <VisualState x:Name="PointerOver">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="#40FFFFFF"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Pressed">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="BorderElement" Storyboard.TargetProperty="Background">
                                                <DiscreteObjectKeyFrame KeyTime="0" Value="#80FFFFFF"/>
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Disabled"/>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border x:Name="BorderElement" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border x:Name="RootBorder" Padding="0" Background="Transparent" SizeChanged="RootBorder_SizeChanged">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <!-- 左滚动 -->
                <ColumnDefinition Width="*"/>
                <!-- 标签滚动区 -->
                <ColumnDefinition Width="Auto"/>
                <!-- 右按钮组 -->
                <ColumnDefinition Width="*"/>
                <!-- 拖拽区域 -->
            </Grid.ColumnDefinitions>

            <!-- 左滚动按钮 -->
            <Button x:Name="ScrollLeftBtn" Grid.Column="0" Content="&#xE76B;"
                    Style="{StaticResource TabToolButtonStyle}" Width="24" Height="24"
                    Click="ScrollLeftBtn_Click" VerticalAlignment="Center" Margin="2,0,0,0" Visibility="Collapsed"/>

            <!-- 标签滚动区 -->
            <ScrollViewer x:Name="TabScrollViewer" Grid.Column="1"
                          HorizontalScrollBarVisibility="Hidden"
                          VerticalScrollBarVisibility="Disabled"
                          VerticalAlignment="Stretch"
                          HorizontalScrollMode="Enabled"
                          Background="Transparent"
                          ViewChanged="TabScrollViewer_ViewChanged">
                <StackPanel x:Name="TabBarPanel" Orientation="Horizontal"
                            VerticalAlignment="Stretch" Background="Transparent" />
            </ScrollViewer>

            <!-- 右滚动按钮 + 新建标签按钮 + 标题栏拖拽区 -->
            <StackPanel x:Name="RightSidePanel" Grid.Column="2"
                        Orientation="Horizontal" VerticalAlignment="Center"
                        Background="Transparent" Margin="0,0,50,0">
                <Button x:Name="ScrollRightBtn" Content="&#xE76C;"
                        Style="{StaticResource TabToolButtonStyle}" Width="24" Height="24"
                        Click="ScrollRightBtn_Click" Visibility="Collapsed" Margin="0,0,0,0"/>
                <Button x:Name="NewTabButton" Content="&#xE710;"
                        Style="{StaticResource TabToolButtonStyle}" Width="32" Height="32"
                        Click="NewTabButton_Click" Margin="0,0,0,0"/>
            </StackPanel>

            <!-- 标题栏可拖拽区域（占满剩余空间） -->
            <Rectangle x:Name="TitleBarDragArea" Fill="Transparent"
                       Grid.Column="3" VerticalAlignment="Stretch"/>
        </Grid>
    </Border>
</UserControl>