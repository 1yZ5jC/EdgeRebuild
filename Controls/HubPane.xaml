<UserControl
    x:Class="EdgeRebuild.Controls.HubPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="Transparent"
    IsHitTestVisible="True"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch">

    <Grid Background="Transparent" IsHitTestVisible="True">
        <muxc:NavigationView
            x:Name="HubNavView"
            PaneDisplayMode="Left"
            OpenPaneLength="160"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            AlwaysShowHeader="False"
            SelectionChanged="HubNavView_SelectionChanged"
            Background="Transparent"
            IsHitTestVisible="True"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">

            <muxc:NavigationView.MenuItems>
                <muxc:NavigationViewItem Content="收藏夹" Tag="Favorites">
                    <muxc:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE734;" FontFamily="Segoe MDL2 Assets"/>
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>

                <muxc:NavigationViewItem Content="历史记录" Tag="History">
                    <muxc:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE81C;" FontFamily="Segoe MDL2 Assets"/>
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>

                <muxc:NavigationViewItem Content="下载" Tag="Downloads">
                    <muxc:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE896;" FontFamily="Segoe MDL2 Assets"/>
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>
            </muxc:NavigationView.MenuItems>

            <ScrollViewer IsHitTestVisible="True" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                <StackPanel x:Name="ContentPanel" Orientation="Vertical"
                            HorizontalAlignment="Stretch">
                    <StackPanel x:Name="FavStackPanel" Visibility="Visible" Orientation="Vertical"
                                HorizontalAlignment="Stretch"/>
                    <StackPanel x:Name="HistoryStackPanel" Visibility="Collapsed" Orientation="Vertical"
                                HorizontalAlignment="Stretch"/>
                    <StackPanel x:Name="DownloadsStackPanel" Visibility="Collapsed" Orientation="Vertical"
                                HorizontalAlignment="Stretch"/>
                </StackPanel>
            </ScrollViewer>
        </muxc:NavigationView>
    </Grid>
</UserControl>